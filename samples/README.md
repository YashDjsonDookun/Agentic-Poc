# Sample / mock data (R.4)

Add mock tickets and sample SOPs here for demos. No real sensitive data.

- **tickets/** — Mock tickets, templates, issue/incident variants (CSV or JSON per ticket).
- **sops/** — Sample SOPs (markdown or text).

Schema for tickets and SOPs will be documented so drop-in is clear.
